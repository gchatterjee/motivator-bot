{
  "name": "motivator-bot",
  "version": "1.0.0",
  "description": "a bot to motivate",
  "main": "bot.js",
  "author": "Gaurav Chatterjee",
  "scripts": {
    "start": "(([ -f ./auth.json ] || (echo 'Missing credentials; add an auth.json file with token' && false)) && node bot.js) ",
    "lint": "eslint --fix ."
  },
  "dependencies": {
    "discord.js": "^11.2.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
